import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    padding: 10,
    marginTop: 20,
    marginBottom: 10,
  },
  title: {
    marginBottom: 20,
  },
});
